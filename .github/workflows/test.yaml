name: test echos

on:
  push:
    branches: ['main']

jobs:
  test-echos:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: echo-env-vars
        run: |
          echo ${{ GITHUB_PATH }}
          echo ${{ github.ref }}
          echo ${{ github.actor }}
          echo ${{ RUNNER_OS }}
